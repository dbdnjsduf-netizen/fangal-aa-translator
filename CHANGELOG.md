# 업데이트 로그

일반 사용자가 주요 변화를 빠르게 확인할 수 있도록 비슷한 성격의 수정은 하나의 공개 업데이트로 묶었습니다.

## 1.11.3 (2026-08-08) — 수동 방향 우선과 띄어쓰기 대사 인식

- 수동정규식으로 자동 선택된 항목도 일반 **수동** 박스로 가로 번역 대상으로 덮어쓸 수 있습니다.
- 같은 항목을 **세로수동** 박스로 지정하면 기존 수동정규식·가로 감지보다 새 세로 그룹을 우선합니다.
- `雷　龍　波　あ　あ…っ！！`처럼 글자마다 일정한 공백이 들어간 대사를 공백까지 포함한 한 문장으로 자동 선택합니다.
- 말풍선 경계, 반복 간격과 문장성을 함께 확인하며 `ハ　人　ノ　へ…` 같은 AA 구조 행은 자동 대사로 합치지 않습니다.

## 1.11.2 (2026-08-08) — 2차원 AA 그림 문맥 감지

- `二·ニ·ﾆ·一·ー`가 `＝＿─━…` 같은 선·점과 섞여 반복되는 구조를 정규화해 자동 선택에서 제외합니다.
- 세로 후보가 `工·口·日·目·回`처럼 서로 다른 문자여도 AA 골격 문자 비율이 높으면 세로 문장으로 승격하지 않습니다.
- 구조 문자 사이에 기호가 끼어 기존 완전 일치 제외식을 우회하던 문제를 해결했습니다.
- 후보 문자에 직접 연결된 상하좌우·대각선 AA 덩어리를 추적해 한쪽으로 길게 이어진 그림 선도 제외합니다.
- 닫힌 말풍선, 자연스러운 문장, 큰 공백으로 분리된 `あ？·わっ·好き` 같은 짧은 대사는 그림 옆에서도 보호합니다.
- `おーーい　ミリアリア少将ーっ！`처럼 장음과 문장 내부 공백이 함께 있는 대사도 한 문장 전체로 선택합니다.
- `圭圭圭…·州州州…` 같은 반복 한자와 장문의 반각 가타카나가 그림의 명암을 채우는 문자 질감 AA를 감지합니다.
- 반각 가타카나는 무조건 제외하지 않고 주변 AA 덩어리에 연결된 경우에만 그림으로 처리해 실제 반각 대사를 보호합니다.
- 가로로 감지된 항목을 세로수동 박스로 다시 묶거나, 세로 감지 항목을 일반 수동 박스로 가로 문장으로 덮어쓸 수 있습니다.
- 세로 슬롯을 가로로 덮어쓸 때 같은 행의 연속 글자는 하나의 번역 단위로 복원하고 서로 다른 행은 합치지 않습니다.
- 감지 결과를 확실한 대사·수동 확인이 필요한 애매 항목·그림 구성요소로 나눠 전체 선택의 과잉 선택을 줄였습니다.
- 금지항목과 수동정규식은 문자열뿐 아니라 주변 그림 배치 서명도 함께 저장합니다.
- `ニートになった` 같은 실제 가타카나 단어와 자연스러운 세로 대사는 계속 허용합니다.

## 1.11.1 (2026-08-08) — AA 내부 세로열·짧은 대사 감지 보정

- 캐릭터 전체의 멀리 떨어진 외곽 파이프를 세로 말풍선 경계로 사용하는 오인식을 차단했습니다.
- 자동 세로열은 행을 건너뛰지 않고 연속되며, 각 행의 경계 안에 AA 기호가 과도하지 않은 문자 슬롯만 인정합니다.
- 자유형 말풍선도 글자 주변의 가장 가까운 좌우 경계와 행 내부 기호 밀도를 함께 확인합니다.
- `狂ってる…`처럼 한자와 연속 히라가나로 문장성이 충분한 단문은 주변 AA 밀도 억제 대상에서 제외합니다.

## 1.11 (2026-08-08) — GitHub 자동 업데이트

- 상단 버전 버튼에서 현재 설치본과 GitHub `main`의 최신 버전을 자동으로 비교합니다.
- ZIP 설치와 Git 복제 설치 모두 앱 안에서 새 버전을 내려받아 업데이트할 수 있습니다.
- 새 버전은 임시 폴더에서 `npm install`과 배포 빌드를 먼저 통과한 뒤 실제 프로그램에 적용합니다.
- `.env`, API 키, 금지항목과 수동정규식 같은 사용자 설정을 유지하고 서버를 자동 재시작합니다.
- Git 작업 폴더에 수정 중인 파일이 있으면 덮어쓰지 않고 자동 업데이트를 안전하게 중단합니다.
- 오래된 배포본에 남은 루트 임시 테스트 파일은 업데이트 과정에서 정리합니다.

## 1.10 (2026-08-08) — 문자·세로쓰기 자동 감지 정밀화

- 검증된 말풍선 안의 `한자+작은 가나+!?` 짧은 대사가 누락되지 않도록 보완했습니다.
- 박스 밖에서 여러 방향의 AA 획에 둘러싸인 짧은 가나 조각은 캐릭터 그림으로 판단해 전체 선택에서 제외합니다.
- AA 구조 문자표에 잘못 포함되어 있던 한글과 근거 없는 음차 문자열을 제거했습니다.
- `ノ·へ·ハ·つ·二·人`처럼 얼굴과 선에 자주 쓰이는 짧은 모양 문자는 어휘 증거와 주변 여백을 함께 검사합니다.
- `おい`, `危険！` 같은 실제 단문과 위아래로 이어진 자연스러운 장문은 계속 자동 선택합니다.
- 자유형 세로쓰기는 글자 양쪽에 가까운 말풍선 경계가 있고, 행 간격과 좌우 흔들림이 제한된 연속 열만 인정합니다.
- 기존 18칸까지 이어지던 자유형 열 추적을 최대 6칸으로 줄이고 전체 열의 좌우 범위도 제한했습니다.
- 금지항목의 짧은 AA 오탐과 매우 비슷한 후보는 전체 선택에서 학습 제외합니다.
- 수동정규식의 긍정 예시는 유사 금지 패턴 때문에 실제 대사가 과잉 제외되지 않도록 보호합니다.
- 학습 보정은 짧고 어휘 근거가 약한 후보에만 적용하며 자연스러운 장문에는 일반화하지 않습니다.

## 1.9 (2026-08-08) — Codex 로그인·OpenRouter 번역 엔진 추가

- Codex CLI에서 ChatGPT 계정으로 로그인한 구독 할당량을 이용해 `gpt-5.6-luna`로 번역할 수 있습니다.
- OpenRouter 개인 API 키로 `google/gemma-3-27b-it` 모델을 사용할 수 있습니다.
- Codex OAuth 인증 파일을 앱이 직접 읽거나 복사하지 않고 격리된 `codex exec`에 번역 실행을 위임합니다.
- OpenRouter 키는 현재 브라우저 탭에만 보관하며 로컬 Node 서버가 요청 순간에만 중계합니다.
- 새 엔진에도 항목 수 검증, 일본어 잔존 재시도, 실패 청크 분할과 부분 완료 처리를 동일하게 적용합니다.
- 기존 AA 표기·공백 배치와 현재 Gemini 모델·호출 방식은 변경하지 않았습니다.

## 1.8 (2026-08-06) — 수동 정규식 오선택과 짧은 번역 배치 개선

- 일반 수동 정규식은 원문의 좌우가 공백·줄바꿈 또는 문서 끝으로 분리된 경우에만 자동 선택합니다.
- 선택 대상이 다른 단어나 AA 문자에 붙어 있으면 일치시키지 않아 과잉 선택을 방지합니다.
- 화면 표시용 세그먼트가 나뉘어 있어도 실제 앞뒤 문자를 이어서 검사합니다.
- 일반·수동 번역문이 원문보다 짧으면 부족한 표시 폭만큼 뒤에 공백을 추가해 주변 AA 위치를 유지합니다.

## 1.7 (2026-08-02) — 화이트 모드와 반복 원문 자동 선택

- 상단 버튼으로 AA 작업 화면을 화이트·다크 모드로 즉시 전환하고 브라우저에 설정을 저장합니다.
- 하단의 **수동정규식**에서 박스로 지정한 원문을 안전한 완전 일치 정규식으로 저장합니다.
- 저장된 원문은 현재 파일과 다음 파일에서 모두 청록색 번역 대상으로 자동 선택됩니다.
- 상단의 **수동정규식 관리**에서 개별·전체 삭제와 JSON 백업·복원을 지원합니다.
- 수동 정규식과 금지 목록이 겹치면 금지 목록을 우선해 의도치 않은 번역을 막습니다.
- 이미지 ZIP 내보내기에 캔버스 재사용과 메모리 크기 기반 병렬 인코딩을 적용했습니다.

## 1.6 (2026-08-02) — 반복 오인식 금지 목록 추가

- 하단의 **금지하기**로 선택된 일반 문장이나 세로쓰기 문장을 금지 목록에 저장합니다.
- 완전히 같은 원문이 반복되면 현재 파일에서 모두 해제하고 다음 파일의 전체 선택에서도 제외합니다.
- 상단의 **금지항목 관리**에서 개별 삭제, 전체 삭제와 JSON 백업·복원을 지원합니다.
- 금지 목록은 브라우저에 영구 저장되며 번역 파일이나 API 키와는 분리됩니다.

## 1.5 (2026-08-01) — 이미지 ZIP 다운로드 추가

- 상단 다운로드 옆에 이미지 다운로드와 PNG/JPG 설정 창을 추가했습니다.
- 페이지 높이·여백·스마트 분할·너비 자동 최적화·좌측 공백 제거 설정을 지원합니다.
- 이미지마다 Saitamaar 글꼴로 렌더링하고 순번 파일을 하나의 ZIP으로 묶습니다.
- README에 Node.js LTS 설치 절차를 추가하고 실행 스크립트가 Node.js·npm·버전을 자동 검사합니다.

## 1.4 (2026-08-01) — AA 화면 표시 개선

- 선택 강조 표시가 문자 사이에 여백을 추가하던 문제를 수정했습니다.
- 전용 AA 뷰어와 동일한 Saitamaar 우선 글꼴과 1.125 줄높이를 적용했습니다.
- 선택을 위해 텍스트가 여러 조각으로 나뉘어도 배치가 달라지지 않도록 조정했습니다.

## 1.3 (2026-08-01) — 번역문 공백 배치 개선

- 오른쪽 여유 공간을 먼저 사용하고, AA가 밀려날 때만 왼쪽 공백으로 확장합니다.
- 공간이 부족해도 번역은 적용하고 실제로 밀려난 부분만 안내합니다.

## 1.2 (2026-08-01) — Ollama·Gemini 모델 선택 추가

- Ollama의 구독형 Gemma와 로컬 TranslateGemma를 선택할 수 있습니다.
- Gemini의 Flash·Flash-Lite 모델을 선택할 수 있습니다.

## 1.1 (2026-08-01) — 번역 오류와 선택 문제 해결

- 일부 청크나 세로쓰기 번역이 실패해도 정상 결과는 끝까지 적용합니다.
- 항목 수 불일치와 남은 일본어를 자동으로 나누어 다시 번역합니다.
- 세로쓰기 위치·오인식과 수동 드래그 선택 문제를 개선했습니다.

## 1.0 (2026-08-01) — Fangal AA 번역기 공개

- 일본어 AA의 그림과 대사 위치를 최대한 유지하는 한국어 번역 기능을 공개했습니다.
- Ollama·Gemini API, 스마트 선택, 세로쓰기 번역을 지원합니다.
